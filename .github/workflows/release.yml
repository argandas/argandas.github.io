name: release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch: {}

jobs:
  release:
    runs-on: ubuntu-latest
    container: texlive/texlive:latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3
      - name: Compile LaTex to PDF
        run: make
      - name: Set env variables
        run: |
          apt-get update
          apt-get install -y jq
          USER_NAME=$(jq -r '.basics.name' resume.json | sed -r 's/ /_/g')
          echo "FILE_NAME=${USER_NAME}_Resume_${{github.ref_name}}.pdf" >> $GITHUB_ENV
      - shell: bash
        run: |
          cp .build/resume.pdf ${{ env.FILE_NAME }}
      - name: Release
        uses: softprops/action-gh-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          files: |
            ${{ env.FILE_NAME }}
            .build/resume.pdf
          draft: false
          prerelease: false
